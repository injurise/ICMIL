# Benchmark datasets and model checkpoints ship WITH this repository — they are
# deliberately tracked, not ignored. Only reproduction outputs are transient.
results/

# Python
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
*.egg-info/
